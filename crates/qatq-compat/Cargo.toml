[package]
name = "qatq"
version = "0.1.0"
edition = "2021"
description = "PermeantOS in-tree compatibility shim for the external QATQ crate"
license = "Apache-2.0"

[lib]
name = "qatq"
path = "src/lib.rs"
